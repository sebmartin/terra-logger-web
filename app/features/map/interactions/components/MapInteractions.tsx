import { useFeatureClick } from "@/app/hooks/useFeatureClick";

export function MapInteractions() {
  // This will handle the switching bewteen display and editing interactions

  // Enable feature click interactions
  useFeatureClick();

  return null;  // No UI
}
